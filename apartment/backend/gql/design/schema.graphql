type Article {
  Name: string!
}

type Query {
  articles(): [Article]!
  article(): Article!
}

enum LoginResult {
  Success
  Error
}

type LoginInfo {
  result: LoginResult!
  token: string!
}

type Mutation {
  login(login: string!, password: string!): LoginInfo!
}
